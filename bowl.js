import * as net from 'node:net';
import * as fs from "node:fs";
import * as path from "node:path";

//This is the push side of a Pipa. It needs the following:
//A client connection to host on a port number belonging to the host 
//A bowl (just a file) to socket pipa
export class Bowl {
  constructor(){

  }
}
  
